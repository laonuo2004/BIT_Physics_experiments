# Usage

## Case 1: Console Input

Run command `./calc.exe` and input the data according to the prompt.

## Case 2: File Input

Run command `./calc.exe data.in`, where `data.in` is the path to your input file.